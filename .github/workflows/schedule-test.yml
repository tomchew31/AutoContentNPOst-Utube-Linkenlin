name: Schedule Test

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes, purely for testing
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Schedule fired at $(date -u)"
